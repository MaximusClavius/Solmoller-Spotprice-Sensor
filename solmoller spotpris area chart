type: custom:apexcharts-card
apex_config:
  chart:
    height: 400px
graph_span: 7d
span:
  start: day
now:
  show: true
  label: Nu
header:
  title: Prognose på spotpriser (kr/kWh)
  show: true
experimental:
  color_threshold: true
series:
  - entity: sensor.prognose_pa_el_priser_for_vestdanmark_dk1
    type: area
    opacity: 0.7
    stroke_width: 2
    data_generator: |
      var data = entity.attributes.DK1.map((start, index) => {
        return [new Date(start["Time"]).getTime(), entity.attributes.DK1[index]["Price"] / 100 * 1.25];
      });
      return data;
    color_threshold:
      - value: 0
        color: green
      - value: 0.35
        color: yellow
      - value: 0.45
        color: orange
      - value: 0.55
        color: red
